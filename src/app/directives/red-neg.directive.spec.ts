import { RedNegDirective } from './red-neg.directive';

describe('RedNegDirective', () => {
  it('should create an instance', () => {
    const directive = new RedNegDirective();
    expect(directive).toBeTruthy();
  });
});
